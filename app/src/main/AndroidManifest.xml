<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kotlincashloan">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service android:name=".ui.notification.PushNotification">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".ui.registration.login.HomeActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.kotlinscreenscanner.ui.MainActivity"
            android:launchMode="singleTop"/>
        <activity android:name="com.example.kotlinscreenscanner.ui.login.QuestionnaireActivity"
            android:launchMode="singleTop"/>
        <activity android:name="com.example.kotlinscreenscanner.ui.login.NumberActivity"
            android:launchMode="singleTop"/>
        <activity android:name="com.example.kotlincashloan.ui.registration.recovery.PasswordRecoveryActivity"
            android:launchMode="singleTop"/>
        <activity android:name="com.example.kotlincashloan.ui.registration.recovery.ContactingServiceActivity"
            android:launchMode="singleTop"/>
    </application>

</manifest>